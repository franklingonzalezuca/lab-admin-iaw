@model IEnumerable<Laboratorio.Models.tipoReservacion>

@{
    ViewBag.Title = "Todos los elementos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Todos los elementos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Reservación
        </th>
        <th>
            Descripcion
        </th>
        <th>
            Activo
        </th>
        <th>Acciones</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreReservacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idTipoReservacion }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idTipoReservacion }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.idTipoReservacion })
        </td>
    </tr>
}

</table>
