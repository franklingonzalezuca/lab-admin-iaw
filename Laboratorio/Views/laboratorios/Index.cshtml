@model IEnumerable<Laboratorio.Models.laboratorio>

@{
    ViewBag.Title = "Todos los elementos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Todos los elementos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Laboratorio
        </th>
        <th>
            Activo
        </th>
        <th>Acciones</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreLaboratorio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idLaboratorio }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idLaboratorio }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.idLaboratorio })
        </td>
    </tr>
}

</table>
